# Copy to .env_x and fill in. Used by tweet_daily.py (X bot).
# X: developer.x.com → your app → Keys and tokens (generate Access Token for your user).
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
X_BEARER_TOKEN=

# OpenAI (LangChain tweet generation + DALL·E 3 satire images)
OPENAI_API_KEY=

# Optional: Tavily search for standalone/trending tweets (tavily.com)
# TAVILY_API_KEY=
